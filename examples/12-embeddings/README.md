# Embedding completion example

## Pre-requisites

- Install Docker Model Runner
- Pull the model image:
  ```bash
  docker model pull ai/mxbai-embed-large
  ```

## Running the Example

```bash
cd examples/12-embeddings
go run main.go
```